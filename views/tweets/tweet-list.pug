extends ../layout.pug

block content
  div(class="d-flex flex-row content mx-auto")      
    div(class="flex-fill d-flex flex-column")
      if tweets
        each tweet in tweets
          div(class="card tweet-element mb-3")
            div(class="card-body")= tweet.content